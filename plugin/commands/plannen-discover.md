---
description: Discover events from your saved sources and the web — does not auto-create events.
argument-hint: "<query>"
---

The user has invoked `/plannen-discover` with query: `$ARGUMENTS`.

Trigger the `plannen-discovery` skill and follow its workflow exactly. If `$ARGUMENTS` is empty, ask the user what they're looking for.

Combine `search_sources` results (saved organisers/platforms) with web search for broader coverage. Note provenance per result. Do not auto-create events — finish with the offer *"Want me to save any of these as planned events?"*.
